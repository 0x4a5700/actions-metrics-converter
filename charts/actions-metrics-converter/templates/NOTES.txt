{{ .Chart.Name }} has been installed.

{{- if .Values.ingress.enabled }}

GitHub webhook URL:
  http{{ if .Values.ingress.tls.secretName }}s{{ end }}://{{ .Values.ingress.host }}/
{{- else }}

Ingress is disabled. To reach the service from your machine:

  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "actions-metrics-converter.fullname" . }} 3018:{{ .Values.service.port }}

To expose it through Contour, set ingress.enabled=true and ingress.host.
{{- end }}

Configure the GitHub webhook with content type application/json and the same
secret as githubWebhookSecret, subscribed to workflow_job and workflow_run events.
