apiVersion: v1
kind: Service
metadata:
  name: {{ include "actions-metrics-converter.fullname" . }}
  labels:
    {{- include "actions-metrics-converter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "actions-metrics-converter.selectorLabels" . | nindent 4 }}
